import React from "react";

const Connection = () => {
  return (
    <div className="connection">
      <img src="" alt="" />
      <div className="status">
        {/* {connected ? `Connected To ${admin1}` : `Not Connected to ${admin2}`} */}
      </div>
    </div>
  );
};

export default Connection;
